using System;
using System.Collections.Generic;
using System.IO;
using System.Text;

namespace HyperspaceBoringMachine
{
    public static class Constants
    {
        public static string configDirPath = Path.GetDirectoryName(System.Reflection.Assembly.GetExecutingAssembly().Location);
    }
}
